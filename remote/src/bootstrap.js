import React from "react";
import ReactDOM from "react-dom";
import Button from "./Button";

// Optional: Render something locally for testing
ReactDOM.render(<Button />, document.getElementById("root"));
